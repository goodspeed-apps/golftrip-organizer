/**
 * GAS Template, Toast (re-export)
 *
 * This file re-exports from the canonical implementation at components/ui/Toast.tsx.
 * Import from either path, they resolve to the same provider and hook.
 */

export { ToastProvider, useToast } from './ui/Toast';
export type { ToastType, ToastAction, ToastItem, ToastContextValue } from './ui/Toast';
